version: 2

models:
  - name: stg_fred_macroeconomic_data
    columns:
      - name: date
        tests:
          - not_null
          - unique

  - name: stg_lending_club
    columns:
      - name: loan_id
        tests:
          - not_null
          - unique
      - name: loan_status
        tests:
          - not_null
      - name: loan_issue_date
        tests:
          - not_null
